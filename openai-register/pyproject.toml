[project]
name = "openai-register"
version = "0.1.0"
description = "OpenAI 自动注册脚本"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "curl_cffi",
    "requests",
]

[project.optional-dependencies]
cpa = ["aiohttp"]
