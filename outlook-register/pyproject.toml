[project]
name = "outlook-register"
version = "0.1.0"
description = "Outlook 自动注册脚本"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "playwright",
    "requests",
    "faker",
]

[tool.uv]
# 保持默认，无特别配置
